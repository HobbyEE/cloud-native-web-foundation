{
  "name": "web-firebase",
  "version": "1.0.0",
  "description": "The web service's Firebase project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "firebase:login": "firebase login",
    "deploy": "firebase deploy"
  },
  "author": "Nick Kalousis",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^9.20.0"
  }
}
